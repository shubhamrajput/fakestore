<!DOCTYPE html>
<html lang="en">
  <%- include('head')  %>
  <body>
    <%- include('navbar')  %>
    <div class="jumbotron" style="margin-top:100px;">
      <div class="container">
        <div class="row">
          <% items.forEach(function(item){%>
          <div class="col-sm">
            
            <div class="card" style="width: 20rem;">
              <img class="card-img-top" src="" alt="Card image cap" style="height:10rem;">
              <div class="card-body">
                <h5 class="card-title"><%= item.name %></h5>
                <h6> Quantity:<%= item.quantity %></h6>
                <p class="card-text"><%= item.description %></p>

                
              </div>
            </div>
            
          </div>
          <% }); %>
        </div>
      </div>
    </div>
    
  </body>
</html>